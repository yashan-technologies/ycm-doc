为提高业务隔离性，减少误操作，建议为YashanDB产品创建独立的安装用户，以下操作须在所有服务器上执行。

1. 创建安装用户。

    本安装手册中，以yashan作为数据库安装用户名称。

    ```shell
    # useradd -d /home/yashan -m yashan
    # passwd yashan
    ```

2. 新建YASDBA用户组并将安装用户加入该组。

    ```shell
    # groupadd YASDBA
    # usermod -a -G YASDBA yashan
    ```
